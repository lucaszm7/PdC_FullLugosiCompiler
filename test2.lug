main
{
    var int x;
    var float y;
    var float z;
    x := 10;
    y := 34.92E-23;
    z := (x / y);

    do
    {
        z := (z + 2.3E+2);

        if ((z == y))
        {
            print(z);
        };
    } while ((z < 23.5E+28));

    return 0;
}